local p=game.Players.LocalPlayer local g=Instance.new("ScreenGui",p:WaitForChild("PlayerGui")) g.ResetOnSpawn=false local keyValid=false local expire=0 local admin=false local function createMain() local m=Instance.new("Frame",g) m.Size=UDim2.new(0,260,0,320) m.Position=UDim2.new(0.5,-130,0.5,-160) m.BackgroundColor3=Color3.fromRGB(0,0,0) m.BackgroundTransparency=0.2 m.Active=true m.Draggable=true local t=Instance.new("TextLabel",m) t.Size=UDim2.new(1,0,0,35) t.Text="PINKKY" t.TextColor3=Color3.new(1,1,1) t.BackgroundTransparency=1 local v1=Instance.new("TextButton",m) v1.Size=UDim2.new(0.7,0,0,40) v1.Position=UDim2.new(0.15,0,0.2,0) v1.Text="V1" v1.BackgroundColor3=Color3.fromRGB(120,120,120) local v2=Instance.new("TextButton",m) v2.Size=UDim2.new(0.7,0,0,40) v2.Position=UDim2.new(0.15,0,0.4,0) v2.Text="V2" v2.BackgroundColor3=Color3.fromRGB(120,120,120) local p1=Instance.new("Frame",m) p1.Size=UDim2.new(1,0,1,0) p1.BackgroundColor3=Color3.fromRGB(0,0,0) p1.BackgroundTransparency=0.2 p1.Visible=false local b1=Instance.new("TextButton",p1) b1.Text="<" b1.Size=UDim2.new(0,35,0,35) b1.BackgroundColor3=Color3.fromRGB(120,120,120) local autoBtn=Instance.new("TextButton",p1) autoBtn.Text="Auto Farm V1" autoBtn.Size=UDim2.new(0.7,0,0,40) autoBtn.Position=UDim2.new(0.15,0,0.3,0) autoBtn.BackgroundColor3=Color3.fromRGB(120,120,120) local farming=false local function getNearestPart() local char=p.Character or p.CharacterAdded:Wait() local hrp=char:WaitForChild("HumanoidRootPart") local nearest=nil local dist=math.huge for _,v in pairs(workspace:GetDescendants()) do if v:IsA("BasePart") and not v:IsDescendantOf(char) then local d=(v.Position-hrp.Position).Magnitude if d<dist and d>5 then dist=d nearest=v end end end return nearest end autoBtn.MouseButton1Click:Connect(function() farming=not farming autoBtn.Text=farming and "STOP" or "Auto Farm V1" if farming then task.spawn(function() while farming do local part=getNearestPart() if part then local char=p.Character or p.CharacterAdded:Wait() local hrp=char:WaitForChild("HumanoidRootPart") hrp.CFrame=part.CFrame+Vector3.new(0,5,0) end task.wait(1) end end) end end) local p2=Instance.new("Frame",m) p2.Size=UDim2.new(1,0,1,0) p2.BackgroundColor3=Color3.fromRGB(0,0,0) p2.BackgroundTransparency=0.2 p2.Visible=false local b2=Instance.new("TextButton",p2) b2.Text="<" b2.Size=UDim2.new(0,35,0,35) b2.BackgroundColor3=Color3.fromRGB(120,120,120) local tog=Instance.new("TextButton",p2) tog.Text="V2." tog.Size=UDim2.new(0.7,0,0,40) tog.Position=UDim2.new(0.15,0,0.3,0) tog.BackgroundColor3=Color3.fromRGB(120,120,120) local hid={} local off=false local function hide() hid={} for _,v in pairs(workspace:GetDescendants()) do if v:IsA("BasePart") and not v:IsDescendantOf(p.Character) then table.insert(hid,v) v.Transparency=1 elseif v:IsA("Decal") or v:IsA("Texture") then v.Transparency=1 end end end local function show() for _,v in pairs(hid) do if v then v.Transparency=0 end end end tog.MouseButton1Click:Connect(function() off=not off tog.Text=off and "RESTORE" or "V2." if off then hide() else show() end end) v1.MouseButton1Click:Connect(function() v1.Visible=false v2.Visible=false p1.Visible=true end) v2.MouseButton1Click:Connect(function() v1.Visible=false v2.Visible=false p2.Visible=true end) b1.MouseButton1Click:Connect(function() p1.Visible=false v1.Visible=true v2.Visible=true end) b2.MouseButton1Click:Connect(function() p2.Visible=false v1.Visible=true v2.Visible=true end) end local keyUI=Instance.new("Frame",g) keyUI.Size=UDim2.new(0,250,0,150) keyUI.Position=UDim2.new(0.5,-125,0.5,-75) keyUI.BackgroundColor3=Color3.fromRGB(0,0,0) keyUI.BackgroundTransparency=0.2 local lbl=Instance.new("TextLabel",keyUI) lbl.Size=UDim2.new(1,0,0,40) lbl.Text="ใส่ KEY (ติดต่อเจ้าของ)" lbl.TextColor3=Color3.new(1,1,1) lbl.BackgroundTransparency=1 local box=Instance.new("TextBox",keyUI) box.Size=UDim2.new(0.8,0,0,40) box.Position=UDim2.new(0.1,0,0.4,0) local ok=Instance.new("TextButton",keyUI) ok.Text="ENTER" ok.Size=UDim2.new(0.5,0,0,35) ok.Position=UDim2.new(0.25,0,0.75,0) ok.MouseButton1Click:Connect(function() if box.Text=="1536" then keyValid=true admin=false expire=os.time()+86400 keyUI:Destroy() createMain() task.spawn(function() while keyValid do if os.time()>expire then keyValid=false g:ClearAllChildren() break end task.wait(1) end end) elseif box.Text=="0935708182" then keyValid=true admin=true keyUI:Destroy() createMain() end end)
